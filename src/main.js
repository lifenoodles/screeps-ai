/**
 * Needed something to test.
 * @param x
 * @returns {x}
 */
function proofOfConcept(x) {
    return x;
}
module.exports.proofOfConcept = proofOfConcept;

/**
 * Main entry point, set up configuration and start engine.
 */
module.exports.loop = function () {
    console.log("hello world");
};